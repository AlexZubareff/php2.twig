<html>
<link rel='stylesheet' href= style.css>
<head>
<title>{{ title }}</title>
</head>
<body>
<div class = "image_galery">       
 {# Выводим блок с картинками #}
{% for image in images %}
    <div class = "image_item">  
    {# отображаем картинки и в href прописываем ссылку на страницу photo.php и подставляем из полученных данных id картинки #}
        <a target="_blanc" href="photo.php?id={{image.id}}"><img id="{{image.id}}" src="{{image.path}}{{image.filename}}" width="320" height="180" ></a> 
        <h4>Число кликов по картинке: {{image.vote_count}}<h4/>
    </div>
{% endfor %}
</div>
</body>
</html>
